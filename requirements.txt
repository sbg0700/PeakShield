# ---- Core data / ML ----
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
optuna>=3.4
tqdm>=4.66
pyarrow>=14.0          # parquet I/O for intermediate pipeline artifacts

# ---- Visualization (used by EDA notebooks; src stays plot-free) ----
matplotlib>=3.7
seaborn>=0.13

# ---- Dashboard (Flask realtime app) ----
flask>=3.0
requests>=2.31

# ---- Optional (present in the original notebook environment) ----
lightgbm>=4.0
